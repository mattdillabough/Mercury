import React from 'react';
import { Text } from 'react-native';

import { Screen } from '../components';

function ApplicationScreen(props) {
    return (
        <Screen>
            <Text>Applications</Text>
        </Screen>
    );
}

export default ApplicationScreen;